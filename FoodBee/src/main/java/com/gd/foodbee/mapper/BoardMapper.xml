<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gd.foodbee.mapper.BoardMapper">
	<insert id="insertBoard" parameterType="com.gd.foodbee.dto.BoardDTO">
	<selectKey resultType="int" order="AFTER" keyProperty="boardNo">
		SELECT LAST_INSERT_ID()
	</selectKey>
		INSERT INTO board (
			title,
			board_category,
			create_datetime,
			content,
			board_pw
		) VALUES (
			#{title},
			#{boardCategory},
			NOW(),
			#{content},
			SHA2(#{boardPw},256)
		)
	
	</insert>
	<select id="selectBoardList" resultType="Map">
		SELECT 
			b.board_no boardNo,
			b.title,
			b.board_category boardCategory, 
			b.view,
			COUNT(bc.comment_no)AS commentCnt,
			DATE_FORMAT(b.create_datetime,'%Y-%m-%d %H:%i') AS createDatetime 
		FROM board b 
		LEFT JOIN board_comment bc ON b.board_no = bc.board_no 
		GROUP BY b.board_no
	
	</select>



</mapper>